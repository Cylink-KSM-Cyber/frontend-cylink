import HomeTemplate from "@/components/templates/HomeTemplate";

/**
 * Home page component
 * @description The main home page that uses the HomeTemplate
 * @returns Home page component
 */
export default function Home() {
  return <HomeTemplate />;
}
